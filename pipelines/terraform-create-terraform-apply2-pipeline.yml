trigger:
  - main

resources:
  repositories:
    - repository: templates
      type: github
      name: bigcat3997/one-press-ado-templates
      endpoint: github.com_BigCat3997
      ref: refs/heads/tidy
    - repository: terraform-ado
      type: github
      name: bigcat3997/pinwheel-ado
      endpoint: github.com_BigCat3997
      ref: refs/heads/main

parameters:
  - name: environment
    displayName: Environment
    type: string
    default: dev
    values:
      - dev
      - stg
      - prd

extends:
  template: templates/workflow-terraform-apply4.yml@templates
  parameters:
    runningAgent: ubuntu-latest
    dependencies:
      - checkout: self
      - checkout: terraform-ado
    tfWorkDir: pinwheel-ado/modules/impl/pipeline
    tfVarsFile: environments/${{ parameters.environment }}.tfvars
    orgServiceUrl: $(AZDO_ORG_SERVICE_URL)
    personalAccessToken: $(AZDO_PERSONAL_ACCESS_TOKEN)
    adoProject: $(System.TeamProject)
    artifactFeed: terraform-state
    statePackageName: tfstate-impl-pipeline-${{ parameters.environment }}
